spring:
  application:
    name: clearing-settlement-service
  config:
    import: optional:configserver:http://localhost:8888

# 로컬 테스트용 최소 설정 (Config Server를 못 찾을 경우 대비)
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
